// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/francogalfre/coop/apps/relay/internal/db/ent/agentsession"
	"github.com/francogalfre/coop/apps/relay/internal/db/ent/takeover"
)

// Takeover is the model entity for the Takeover schema.
type Takeover struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// ActorID holds the value of the "actor_id" field.
	ActorID string `json:"actor_id,omitempty"`
	// ActorDisplayName holds the value of the "actor_display_name" field.
	ActorDisplayName string `json:"actor_display_name,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TakeoverQuery when eager-loading is set.
	Edges                  TakeoverEdges `json:"edges"`
	agent_session_takeover *string
	selectValues           sql.SelectValues
}

// TakeoverEdges holds the relations/edges for other nodes in the graph.
type TakeoverEdges struct {
	// Session holds the value of the session edge.
	Session *AgentSession `json:"session,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// SessionOrErr returns the Session value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TakeoverEdges) SessionOrErr() (*AgentSession, error) {
	if e.Session != nil {
		return e.Session, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: agentsession.Label}
	}
	return nil, &NotLoadedError{edge: "session"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Takeover) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case takeover.FieldID, takeover.FieldActorID, takeover.FieldActorDisplayName:
			values[i] = new(sql.NullString)
		case takeover.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case takeover.ForeignKeys[0]: // agent_session_takeover
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Takeover fields.
func (_m *Takeover) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case takeover.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case takeover.FieldActorID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field actor_id", values[i])
			} else if value.Valid {
				_m.ActorID = value.String
			}
		case takeover.FieldActorDisplayName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field actor_display_name", values[i])
			} else if value.Valid {
				_m.ActorDisplayName = value.String
			}
		case takeover.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case takeover.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field agent_session_takeover", values[i])
			} else if value.Valid {
				_m.agent_session_takeover = new(string)
				*_m.agent_session_takeover = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Takeover.
// This includes values selected through modifiers, order, etc.
func (_m *Takeover) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySession queries the "session" edge of the Takeover entity.
func (_m *Takeover) QuerySession() *AgentSessionQuery {
	return NewTakeoverClient(_m.config).QuerySession(_m)
}

// Update returns a builder for updating this Takeover.
// Note that you need to call Takeover.Unwrap() before calling this method if this Takeover
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Takeover) Update() *TakeoverUpdateOne {
	return NewTakeoverClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Takeover entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Takeover) Unwrap() *Takeover {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Takeover is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Takeover) String() string {
	var builder strings.Builder
	builder.WriteString("Takeover(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("actor_id=")
	builder.WriteString(_m.ActorID)
	builder.WriteString(", ")
	builder.WriteString("actor_display_name=")
	builder.WriteString(_m.ActorDisplayName)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Takeovers is a parsable slice of Takeover.
type Takeovers []*Takeover
